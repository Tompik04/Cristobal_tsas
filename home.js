/* VISTA VOUCHERS — placeholder hasta diseñarla */
function renderVouchers(root) {
  root.innerHTML = `
    <p class="view-title">VOUCHERS</p>
    <div class="soon">
      <i class="ti ti-ticket"></i>
      <p>Sección en construcción.</p>
    </div>`;
}
